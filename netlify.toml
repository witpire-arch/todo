# Netlify 설정
# 기존 attendance.html 사이트에 통합하려면 이 파일을 프로젝트 루트에 두세요.

[build]
  functions = "netlify/functions"
  publish = "."

# 스케줄러는 send-notifications.js 안에 schedule('0 0 * * *', ...) 으로 정의되어 있음
# 별도 cron 설정 불필요. KST 오전 9시 = UTC 0시 매일 실행됨.

[functions]
  node_bundler = "esbuild"
